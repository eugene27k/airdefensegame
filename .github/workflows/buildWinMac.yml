name: Build and Publish Game (Windows + macOS)

on:
  workflow_dispatch:  # manual trigger

permissions:
  contents: write     # allow committing builds to repo

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt pyinstaller

      - name: Build executable
        run: |
          pyinstaller --onefile --windowed main.py

      - name: Prepare latest-apps folder
        run: |
          mkdir -p latest-apps
          cp dist/* latest-apps/ || xcopy dist latest-apps /Y

      - name: Commit and push builds
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add latest-apps
          git commit -m "Update latest builds (${{ runner.os }})" || echo "No changes to commit"
          git push
